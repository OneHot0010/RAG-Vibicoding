Refine the chunk for retrieval while preserving factual meaning and source context.
Remove headers, footers, duplicate whitespace, and obvious formatting noise.
Return only the refined chunk text.

Chunk:
{text}
